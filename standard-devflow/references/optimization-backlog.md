# 优化方向 backlog（记录不实施，等待一次性批量落地）

- 状态：仅记录，未实施（2026-08-06，用户决定当前稳定版冻结，暂不改动）
- 稳定基线：当前 main 最新提交 `d15ddbf`（已推送 origin/main）
- 原则：以下各项**不改动任何运行行为**，只作为未来一次性优化的清单；实施时统一落地、统一验证，不逐项小步试错。

## A. 模块并行（收益最大，风险最高）

- 目标：契约冻结（G4）后，无依赖模块 2~3 个并行 spawn，替代当前"每轮单 agent 串行"。
- 改动范围：环境适配层（spawn 协议）、任务书机制（当前"唯一任务书 current.md"需改为每 agent 独立任务书）、总控提示词。
- 前置条件：4447 消息通道真机 E2E 验收通过（当前仅本地验证）；确认并行时各 agent 任务书隔离与写作用域互斥（各写各的 src/modXX + tests/test_modXX，契约注册表只读）。
- 风险：消息投递若仍不可靠会重演"收不到任务"；需先小范围试点（2 个模块）。

## B. 减少重复全量回归

- 目标：dev 轮只跑本模块单测 + 契约测试；全量回归只在装配轮和 G5 跑；去掉每轮"独立复核"（约 2~3 分钟 × N 轮），改由 G5 统一独立复核。
- 改动范围：环境适配层协议一行 + 任务书模板完成标准措辞。
- 风险：低（漏回归由 G5 兜住）。

## C. 任务书预置接口速查（压缩冷启动）

- 目标：把关键接口签名/契约摘要直接写进任务书"上下文摘要"，减少 fork_turns=none 下子 agent 重读 LLD/契约/参考实现的时间（每轮约 2~4 分钟）。
- 改动范围：06-task.md 模板（摘要节结构化：输入文件 → 关键签名速查表）。
- 风险：低（注明"以 LLD/契约为准，摘要过期即上报"）。

## D. model_reasoning_effort 分级

- 目标：主会话保持 max；子 agent 实现/测试/文档轮用 high，评审/调试/装配轮用 max。
- 改动范围：总控提示词加"spawn 时传 reasoning_effort"（spawn_agent 工具原生支持，不传即继承）；不改 config.toml。
- 前置条件：验证 4447 代理透传 `reasoning.effort` 且 DeepSeek 接受 high（低风险，看代理日志请求体即可）。
- 风险：中低；先试 1 个模块对比账本耗时与质量再固化。

## E. 全量回归分片并行

- 目标：全量 unittest 按模块分 3 个进程并行跑，回归从 3~5 分钟压到 1~2 分钟。
- 改动范围：新增并行测试脚本（tests 用随机端口 + 临时目录，已隔离）。
- 风险：低。

## 一次性实施与验证方案（到时按此执行）

1. 在稳定基线上新建分支，A~E 全部落地（含文档/模板/提示词同步三方）。
2. 选一个真实模块或新史诗跑完整流程（需求→G0→…→G5），对比账本耗时基线（2026-08-05 夜：7 模块+装配+G5 约 4.5 小时）与质量（全量测试全绿、门禁记录、watchdog 无幽灵事件）。
3. 验证清单：
   - [ ] 全量 unittest 全绿（当前 387+，含新并行测试脚本）
   - [ ] G5 独立评审 PASS（本地层）
   - [ ] 账本无：孤儿 facts 误报 / 完成后的幽灵 stale 事件 / spawn 后无 interrupt
   - [ ] 并行轮次各 agent 任务书互不覆盖、产物无冲突
   - [ ] reasoning_effort 分级在 4447 日志中可确认
4. 任一验证不达标：回滚到稳定基线 `d15ddbf`，逐项排查后重试。

## 明确不做（不预留）

- CDP / 远程调试端口增强（2026-08-06 决策：与流程适配度低、开源不可复制，不进入 skill）。

## 已知平台边界（记录，不归流程）

- 消息通道（encrypted_content）：已由 REQ-019 4447 解包代理修复；C5（并入正式网关 MOD-03）属 v2 变更。
- agent 进程内部状态/生命周期：平台层能力，流程无法完全替代。
- 长命令包装器（long-cmd.ps1）：只包装原生命令；纯 PS `exit N` 结尾的命令串退出码不可靠（约束已写入模板与协议）。
